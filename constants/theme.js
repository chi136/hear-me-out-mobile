import COLORS from "./colors";

const theme = {
  colors: COLORS,

  fontFamily: {
    serif: "serif",
    sans: "sans-serif",
  },

  borderRadius: {
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    full: 999,
  },

  spacing: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    xxl: 32,
  },
};

export default theme;